export {
    popModalWindow as popModal,
    nextModalWindow as nextModal,
    prevModalWindow as prevModal,
    insertModalWindow as insertModal,
    shiftModalWindow as shiftModal,
    pushModalWindow as pushModal,
    clearModalWindows as clearModals,
} from 'redux-promising-modals';
