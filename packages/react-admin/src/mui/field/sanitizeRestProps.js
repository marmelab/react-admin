export default ({
    addLabel,
    allowEmpty,
    basePath,
    className,
    cellClassName,
    headerClassName,
    source,
    record,
    resource,
    locale,
    textAlign,
    translateChoice,
    ...rest
}) => rest;
