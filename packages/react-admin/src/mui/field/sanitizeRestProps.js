export default ({
    addLabel,
    allowEmpty,
    basePath,
    cellClassName,
    className,
    formClassName,
    headerClassName,
    label,
    locale,
    record,
    resource,
    source,
    textAlign,
    translateChoice,
    ...rest
}) => rest;
