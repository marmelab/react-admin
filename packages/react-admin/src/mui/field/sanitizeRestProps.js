export default ({
    addLabel,
    allowEmpty,
    basePath,
    className,
    cellClassName,
    headerClassName,
    formClassName,
    source,
    record,
    resource,
    locale,
    textAlign,
    translateChoice,
    ...rest
}) => rest;
