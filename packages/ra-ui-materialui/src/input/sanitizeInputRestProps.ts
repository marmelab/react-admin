export const sanitizeInputRestProps = ({
    allowEmpty,
    allowNull,
    alwaysOn,
    basePath,
    component,
    data,
    defaultValue,
    error,
    format,
    formClassName,
    isEqual,
    isRequired,
    label,
    limitChoicesToValue,
    locale,
    meta,
    multiple,
    name,
    options,
    optionText,
    optionValue,
    parse,
    record,
    ref,
    render,
    resource,
    source,
    subscription,
    textAlign,
    translate,
    translateChoice,
    type,
    validate,
    value,
    ...rest
}: any) => rest;
