export default ({
    alwaysOn,
    basePath,
    component,
    defaultValue,
    formClassName,
    handleSubmit,
    initializeForm,
    input,
    isRequired,
    label,
    locale,
    meta,
    options,
    optionText,
    optionValue,
    record,
    resource,
    allowEmpty,
    source,
    submitOnEnter,
    textAlign,
    translate,
    translateChoice,
    ...rest
}) => rest;
