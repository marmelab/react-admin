export default ({
    addLabel,
    allowEmpty,
    basePath,
    cellClassName,
    className,
    formClassName,
    headerClassName,
    label,
    linkType,
    locale,
    record,
    resource,
    sortable,
    source,
    textAlign,
    translateChoice,
    ...rest
}) => rest;
