# Build locally like on github-pages

You must have Ruby installed (https://www.ruby-lang.org/fr/documentation/).

- Copy the `Gemfile` and `Gemfile.lock` files to the root directory
- `bundle exec jekyll build .`