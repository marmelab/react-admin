module.exports = {
    arrowParens: 'avoid',        // Avoid parentheses for single-arg arrow functions
    bracketSpacing: true,        // { foo: bar } instead of {foo: bar}
    bracketSameLine: false,      // Put > of multi-line JSX tags on its own line
    jsxSingleQuote: false,       // Use double quotes in JSX
    printWidth: 80,              // Wrap lines at 80 characters
    quoteProps: 'as-needed',     // Only quote object properties when required
    rangeStart: 0,
    rangeEnd: Infinity,
    semi: true,                  // Add semicolons
    singleQuote: true,           // Use single quotes in JS
    tabWidth: 4,                 // Indent with 4 spaces
    trailingComma: 'es5',        // Add trailing commas where valid in ES5
    useTabs: false               // Use spaces instead of tabs
};
